import "system"
import "math"

class Point(x, y) => {
    function magnitude() => {
        math.sqrt(x * x + y * y)
    }

    function normalize() => {
        var mag = magnitude
        Point(x / mag, y / mag)
    }

    converter string() => "Point({x}, {y})"
    converter list() => [x, y]
}
